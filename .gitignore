# Internal handoff notes (personal context) - not for the public repo
CLAUDE.md

# Build output
bin/
obj/
*.user
*.suo

# Published binary / temp build + release-packaging dirs
publish/
dist/
*.zip

# OS cruft
Thumbs.db
desktop.ini
.DS_Store

# Runtime state lives in %LOCALAPPDATA%\AudioSwitcher\, never in the repo:
#   config.json, overrides.json, crash-log.json, glitch-log.json

# NEVER commit a private signing key (public key lives in Signing.cs; private stays offline).
*.key
*-signing.key
AudioSwitcher-signing.*
manifest.json
manifest.sig
