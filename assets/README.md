# Screenshots to add

Drop these PNGs in this folder (exact filenames), then tell me and I'll wire them into
the main README's Screenshots section:

| Filename | What to capture |
|---|---|
| `tray.png` | The system-tray speaker icon + its right-click menu open (showing status / games / pause / quit). Try to catch it **amber** (a game running) if you can. |
| `control-panel.png` | The GUI control-panel window (double-click the tray icon) — with a game running so the "Running games" and "Learned per-game profiles" lists have content. |
| `console.png` | A terminal running `AudioSwitcher.cmd -Foreground` (or the menu's "Run in this window") showing the ASCII banner + a few colored log lines (`+ Game`, `=> 192000/32`, etc.). |

Tips:
- Crop tight; ~800–1200 px wide is plenty. PNG.
- A dark terminal/theme looks best for the console shot.
- Optional extras welcome: `menu.png` (the `AudioSwitcher.cmd` interactive menu),
  `sound-advanced.png` (Windows Sound → Properties → Advanced showing the rate change).
